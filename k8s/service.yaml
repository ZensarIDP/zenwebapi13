apiVersion: v1
kind: Service
metadata:
  name: zenwebapi13-service
  labels:
    app: zenwebapi13
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: zenwebapi13
